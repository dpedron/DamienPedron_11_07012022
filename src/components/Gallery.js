import Cards from './Cards';
import '../styles/css/Gallery.css';

/**
 * This is the gallery who contain every cards of the homepage
 */

function Gallery() {
  return (
    <section className="gallery">
      <Cards />
    </section>
  );
}

export default Gallery;
